{
  "name": "crashcurse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Crash Curse: a card-grid puzzle prototype with a live tuning panel.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server -d public 8080",
    "sim": "node scripts/sim.js"
  }
}
